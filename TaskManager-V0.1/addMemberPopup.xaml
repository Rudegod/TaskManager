<?xml version="1.0" encoding="utf-8" ?>
<mct:Popup
    x:Class="TaskManager_V0._1.addMemberPopup"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
    xmlns:mct ="clr-namespace:CommunityToolkit.Maui.Views;assembly=CommunityToolkit.Maui"
    CanBeDismissedByTappingOutsideOfPopup="False"
    Size="450, 450">

    <StackLayout>
    
        <Entry x:Name="FullNameEntry" Placeholder="full name" />
        <Entry x:Name="UserNameEntry" Placeholder="user name"/>
        <Entry x:Name="PasswordEntry" Placeholder="password" IsPassword="True"/>
        <Entry x:Name="AgeEntry" Placeholder="age"/>
        <Entry x:Name="PhoneEntry" Placeholder="phone number"/>
        <Entry x:Name="NationalEntry" Placeholder="national"/>
        <Picker x:Name="genderPicker" Title="choose" SelectedIndexChanged="Genderpicker">
            <Picker.ItemsSource>
                <x:Array Type="{x:Type x:String}">
                    <x:String>male</x:String>
                    <x:String>female</x:String>
                </x:Array>
            </Picker.ItemsSource>
        </Picker>
        <Picker x:Name="activeOption" Title="choose" SelectedIndexChanged="activeOptionPicker">
            <Picker.ItemsSource>
                <x:Array Type="{x:Type x:String}">
                    <x:String>active</x:String>
                    <x:String>not active</x:String>
                </x:Array>
            </Picker.ItemsSource>
        </Picker>
            <Button
            BackgroundColor="Black"
            Clicked="addMember"
            HeightRequest="60"
            HorizontalOptions="CenterAndExpand"
            Text="ADD"
            TextColor="red"
            VerticalOptions="EndAndExpand"
            WidthRequest="120" />

    </StackLayout>

</mct:Popup>